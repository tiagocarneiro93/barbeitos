One-sentence: the text entry control; always pair it with `Field` for its label.

```jsx
<Input placeholder="Full name" />
<Input invalid defaultValue="not-an-email" />
<Input multiline rows={5} placeholder="Tell us about the project" />
```

Placeholders are sentence case and concrete. Focus darkens the border to onyx; there is no glow.
