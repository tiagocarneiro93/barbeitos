One-sentence: icon-only square control for toolbars, dialog close, carousel paging.

```jsx
<IconButton icon="x" label="Close" />
<IconButton icon="arrow-right" label="Next project" variant="outline" size="lg" />
<IconButton icon="menu" label="Menu" variant="inverse" />
```
