One-sentence: sets "Building Value. Creating Legacy." in the script face — the signature that closes a page, cover or deck.

```jsx
<Slogan rule align="center" size={52} />
<Slogan text="Created with precision. Designed for timeless impact." size={36} tone="inverse" />
```

Never use `--font-script` for anything else. Never set it below 32px.
