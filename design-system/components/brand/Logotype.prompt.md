One-sentence: the group lockup — use it wherever the brand signs a header, footer or cover.

```jsx
<Logotype size={17} tone="inverse" />                    // navy header
<Logotype size={15} />                                    // paper header
<Logotype size={15} symbol={false} descriptor={false} />  // compact, inner pages
<Logotype size={26} division="R E A L  E S T A T E" />
```

Placeholder mark: no logo file was provided in the sources.
