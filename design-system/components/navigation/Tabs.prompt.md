One-sentence: switches between sibling views of the same object — floor plans, specification, location.

```jsx
<Tabs items={['Overview','Floor plans','Specification','Location']} onChange={setView} />
<Tabs tone="inverse" items={['PT-PT','EN']} defaultValue="EN" />
```
